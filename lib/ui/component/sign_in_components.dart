import 'package:flutter/material.dart';

class SignInComponents {
  static Widget buildSignInTile(context) {
    return Container(
      child: Column(children: <Widget>[
        
      ],),
    );
  }
}
